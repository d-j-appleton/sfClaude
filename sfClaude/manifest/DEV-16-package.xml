<?xml version="1.0" encoding="UTF-8"?>
<!--
    DEV-16: Field History Tracking

    No separate deployment required for this ticket.

    All trackHistory settings are delivered by existing tickets:
      - DEV-8 package.xml: Event__c object (enableHistory) + fields
        Status__c, Start_DateTime__c, End_DateTime__c, Target_Amount__c, Risk_Level__c
      - DEV-10 package.xml: Event_Registration__c object (enableHistory) + fields
        Registration_Status__c, Ticket_Price__c

    Deploy DEV-8 and DEV-10 to activate field history tracking on both objects.
-->
<Package xmlns="http://soap.sforce.com/2006/04/metadata">
    <version>66.0</version>
</Package>
